server:
  port: 0
  forward-headers-strategy: framework

spring:
  application:
    name: user-service
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/momnect
    username: root
    password: mysql1

  feign:
    client:
      config:
        default:
          connectTimeout: 5000
          readTimeout: 5000
      product-service:
        url: lb://PRODUCT-SERVICE
      review-service:
        url: lb://REVIEW-SERVICE

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

jwt:
  secret: testlocaljwtsecretkeyfordevelopmentonlyminimum32characters
  expiration: 1800000           # accessToken 만료 30분 (30 * 60 * 1000)
  refresh-expiration: 604800000 # refreshToken 만료 7일 (7 * 24 * 60 * 60 * 1000)

ftp:
  file-server-url: ""

springdoc:
  packages-to-scan: com.momnect.userservice
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8